[build]
  base    = "apps/web"
  command = "npx pnpm install && npx pnpm run build"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION  = "22"
  VITE_API_URL  = "https://tumaini-api.onrender.com/api/v1"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
